# Product Design QA — 냉톡 반응형 핵심 화면

- source visual truth: `C:/Users/차명근/AppData/Local/Temp/codex-clipboard-60d74c36-3ea3-48fb-b11e-2b01d63e4e84.png`
- implementation screenshot path: `http://localhost:8082/`의 Codex 인앱 브라우저 캡처(탭 5). CUA 캡처는 파일로 영구 저장되지 않아 같은 URL·상태에서 재현한다.
- viewport/state: 666×771px 브라우저 캔버스 안의 9:20 앱 프레임, 로컬 게스트 홈·재고·정렬 팝업·조리도구·레시피 상세
- source size: 390×694px 첨부 이미지
- implementation capture: 앱 본문은 브라우저 높이에서 24px을 뺀 9:20 비율로 계산했다. 주변 회색 캔버스 여백은 비교에서 제외했고 브라우저 CSS 픽셀과 캡처 밀도는 1배율로 확인했다.

## 비교 결과

- 글꼴/타이포그래피: 시스템 한글 글꼴, 진한 페이지 제목과 레시피명을 확인했다. 홈 행동 제목은 프레임 폭에 따라 14·15·17px로 조절되며 한 줄을 유지한다. 레시피명과 제공일은 별도 행과 8px 간격으로 분리했다.
- 간격/레이아웃: 홈의 임박 영역과 두 행동 카드는 2:1로 화면을 채우고, 웹 앱 프레임은 9:20을 유지한다. 조리도구는 실제로 한 행에 3개가 배치되며 긴 이름이 카드 안에서 줄바꿈된다. 좌상단 둥근 홈 버튼, 6개 하단 탭, 흰 카드와 연녹색 태그는 기존 구조를 유지한다.
- 색상: 밝은 회백색 배경, 흰 카드, 짙은 녹색 강조색과 연녹색 태그를 유지했다.
- 이미지/아이콘: 홈 제목 왼쪽에 정식 앱 아이콘을 위한 흰색 빈 영역을 추가했다. 홈·탭·타이머·조리도구는 일관된 Lucide 선형 아이콘을 사용했다.
- 문구: 홈 행동 제목은 `구매내역 등록`, `오늘 뭐 먹지?`로 표시한다. `냉장고 재료 100% 활용`을 제거했고 완료일이 아닌 최초 제공일을 표시한다. 미연결 AI를 실제 기능처럼 보이지 않도록 로컬 검증 안내를 명시했다.
- 주요 상호작용: 임박순 기본 목록, 정렬 팝업 열기, 한국어 이름순 전환(`간장 → 감자 → 계란 → 김치…`), 조리도구 탭, 채팅의 전체 레시피 열기를 브라우저에서 확인했다. 기존 게스트 진입·웹 타이머·차감·재조리 흐름도 유지된다.
- 브라우저 오류: 확인 시 콘솔 오류 0건.

## 비교 이력

1. 첫 구현에서 타이머 시작 직후 10:01로 표시되고, 긴 레시피에서 사용량 검수 영역이 화면 아래에 남았다.
2. 시작 시각을 동일 상태 갱신에 사용하도록 수정하고, 검수 영역 생성 시 스크롤 끝으로 이동하도록 수정했다.
3. 카드의 레시피명·제공일 분리와 저장 레시피 하단 두 버튼을 다시 확인했다.
4. 사용자 캡처에서 홈 임박 재료 네 번째 행이 카드 바깥으로 밀리는 문제를 재현했다. 상단·하단 영역이 동시에 `flex: 1`을 요구해 실제 콘텐츠 높이보다 카드가 작아지는 것이 원인이었다.
5. 임박 카드가 표시 행 수에 맞는 최소 높이를 갖도록 바꾸고 하단 영역의 불필요한 flex를 제거했다. 393px 폭에서 4개 행이 모두 카드 내부에 표시되는 것을 재검증했다.
6. 반복되던 `1. 조리 순서` 제목을 `조리 단계 1` 형식으로 교체했다.
7. 첫 9:20 프레임 적용에서 웹 flex 축약값이 명시 높이를 덮어 콘텐츠가 접히는 P1 문제를 발견했다. grow·shrink·basis를 분리 지정해 전체 높이와 하단 내비게이션을 복원했다.
8. 첫 조리도구 31% 카드 폭에서는 간격을 포함한 세 카드가 한 행에 들어가지 않는 P1 문제를 발견했다. 카드 폭을 30%로 줄이고 내부 글꼴·간격을 조정해 3열과 줄바꿈을 재확인했다.
9. 최종 홈 캡처에서 4개 임박 재료가 카드 안에 있고 날짜가 수량보다 먼저 굵게 표시되며, 하단 행동 영역 아래 불필요한 앱 내부 공백이 없는 것을 확인했다.
10. 사용자 피드백에 따라 임박 행의 순서를 `수량 → 굵은 날짜`로 되돌렸다. 행동 제목을 `구매내역 등록`, `오늘 뭐 먹지?`로 정리하고 프레임 폭별 14·15·17px 크기와 한 줄 제한을 적용했다. 336px 폭의 9:20 앱 프레임에서 두 문구가 줄바꿈·말줄임 없이 표시되는 것을 재확인했다.
11. 임박 재료 노출 수를 프레임 높이에 따라 4·5·6개로 확장했다. 현재 666×752px 브라우저에서는 계산된 328×728px 앱 프레임이 작은 화면 기준에 해당해 4개와 하단 행동 카드가 겹치지 않는 것을 확인했고, 736px·784px 경계에서 각각 5개·6개로 바뀌며 6개를 초과하지 않는 동작은 자동화 테스트로 확인했다.

## 남은 범위

- P0/P1/P2 시각 차이는 발견되지 않았다.
- P3: 실제 브랜드 앱 아이콘이 확정되면 홈의 흰색 빈 영역을 교체한다.
- Google/Supabase/OpenAI/Android 시스템 기능은 별도 통합 단계이므로 이번 로컬 UI QA의 통과 범위가 아니다.

final result: passed
